/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    tools {
        sh 'go-1.22.4'
    }
    stages {
        stage('version') {
            steps {
                go 'go version'
            }
        }
    }
}