/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    tools {
        go '1.22.4'
    }
    stages {
        stage('version') {
            steps {
                sh 'go version'
            }
        }
    }
}